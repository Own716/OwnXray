# Own for Android 更新日志 (Changelog)

# Own for Android v2.6.4 预览版 (v2.6.4-preview)

* 修复测速时节点卡片、底栏与通知栏网速不实时同步的问题
* 修复设置中自定义路由资源后菜单折叠失效的问题
* 修复简单下载模式测速中断与结果不刷新的问题
* 修复部分机场节点 URL 测速误报超时或连接被拒的问题

# Own for Android v2.6.3 预览版 (v2.6.3-preview)

* 靶向修复特定机场节点 URL Test 测速误报（超时/EOF）问题并保持真实低延迟水准不退化
* 放大并发测速 Unix 域套接字保护超时与重试，彻底杜绝 QUIC/UDP 拒绝 (recvmsg: connection refused)
* 优化测速主探测与备用回退超时预算分配，避免协程过早超时中断
* 彻底修复负载均衡设置页面节点列表“半屏滑动”局限，实现全屏顺滑无障碍滚动
* 全面纠正侧边栏“教程文档”设置项文案（更正为“显示直连的速度”并明确手机通知中心提示）
* 教程文档新增“侧边栏网络工具”专题（连通性测试、流媒体解锁、IP纯净度，醒目标注仅供参考）
* 教程文档全量补齐桌面图标、本地备份、WebDAV云同步、流量图表、STUN穿透、DNS泄漏等全系统功能

## 核心修复与体验优化

* **URL Test 测速误报修复与低延迟防退化保护**：
  - 针对特定机场中转节点不支持 HTTP HEAD 请求（返回 EOF、405、403）导致的测速假死/失败，在阶段一握手探活遇到错误时自动使用 HTTP GET 进行兜底预热；
  - 阶段二保活长连接复用算法继续严格保持，纯 1-RTT 网络往返时延测量机制维持在 ~150-250ms 的真实低延迟区间，零退化；
  - 修复多并发测速下向后台进程请求套接字保护（protect）的超时硬编码缺陷，将 100ms 放大至 2 秒并引入 3 次防抖重试，彻底杜绝 Hysteria 2 / TUIC 等 UDP 流量回环进 tun0 触发 QUIC 拒绝（recvmsg: connection refused）；
  - 优化主探测与备用回退探测（gstatic 204）的超时预算配比，并将外层协程等待时间放宽至 `connectionTestTimeout * 2 + 2500L`，确保备用探测链路完整执行。
* **负载均衡页面节点列表“半屏滑动”彻底修复**：
  - 重构 `layout_balancer_settings.xml`，采用固定 Toolbar + `NestedScrollView` 整体协同滚动架构；
  - 彻底解除 RecyclerView 底部高度受限的布局约束，上滑时顶部各项参数设置自然顺滑滚出屏幕，节点列表撑满全屏，带来沉浸式的大列表操作空间。
* **侧边栏“教程文档”纠错与全功能覆盖完善**：
  - 修正“用户界面设置”中文案，精准更正为“显示直连的速度 (showDirectSpeed)”，详细阐明其在“手机通知中心”中展示非代理流量速度的防偷跑机制；
  - 新增“侧边栏网络工具”独立章节，系统化阐述“连通性测试”、“流媒体解锁检测”、“IP 纯净度检测”三大工具，并醒目标注“**这 3 个功能仅供参考**”；
  - 全量补齐桌面图标定制、本地配置备份恢复、WebDAV 云同步、实时流量图表、STUN NAT 穿透、DNS 泄漏检测、负载均衡调度策略等全 APP 系统功能说明；
  - 严格保持【功能说明】、【优劣分析】、【设置推荐】（最稳为原则）三段式排版并健全内置搜索过滤索引。

# Own for Android v2.6.2 预览版 (v2.6.2-preview)

* 修复负载均衡自定义测试 URL 说明汉化与动态展示逻辑
* 规范观测间隔时间单位 (s) 与动态数值展示
* 优化负载均衡节点列表滑动交互为全屏展开
* 新增侧边栏教程文档入口及全设置项深度解析

## 核心修复与体验优化

* **负载均衡“自定义测试 URL”汉化与动态展示逻辑**：
  - 修复多语言字符串缺失，将说明文字恢复为准确易读的中文描述；
  - 增加动态 SummaryProvider：未设置时显示中文引导说明，配置 URL 后自动隐藏说明文字并直接展示所设链接。
* **“观测间隔”数值动态展示与单位规范化**：
  - 标题后缀统一使用字母 `(s)` 明确标注秒数单位（如 `观测间隔 (s)`）；
  - 增加动态展示逻辑：未自定义时直观显示默认数值 `300s`，已自定义时标题下方直接展示用户设定的具体数值，消除长串冗余文案。
* **负载均衡服务器列表滑动交互优化（半屏改全屏）**：
  - 升级布局为 `CoordinatorLayout` + `AppBarLayout`，保持顶部 Toolbar 导航与保存按钮常驻；
  - 顶部设置区域支持协同折叠滑动，向上滑动节点列表时设置区域平滑折叠，使节点列表顺畅撑满全屏，大幅改善浏览与操作体验。
* **侧边栏新增“教程文档”入口及内容体系构建**：
  - 在侧边栏“Telegram 官方频道”与“关于”之间新增“教程文档”入口，图标与间距精细对齐；
  - 系统化覆盖 APP 全量 8 大分类、50+ 项设置，严格按照【功能说明】、【优劣分析】、【设置推荐】三段式结构系统化排版；
  - 推荐原则以“最稳定”为绝对核心，并配备顶部快速即时搜索过滤。

# Own for Android v2.6.1 预览版 (v2.6.1-preview)

* 优化落地 IP 卡片就地静默刷新与负载均衡策略实时联动
* 加固 IPv6 路由规则与虚拟 DNS 防泄露机制
* 修复单节点删除触发重复确认弹窗的问题
* 修复免重载切换节点（Selector）开关偶发失效的问题
* 规范观测间隔（Interval）的秒数单位与默认参数提示
* 优化 URL 测速链路与真实网络连通性对齐

## 核心修复与体验优化

* **负载策略同步与落地 IP 卡片实时刷新失效修复**：
  - 点击底部状态栏刷新按钮直接就地静默触发实时测速与 IP 查询，并辅以 360° 平滑旋转动画，彻底消除额外弹窗打扰；
  - 强制刷新时显式清空旧缓存与销毁客户端连接池，杜绝 TCP Keep-Alive 导致复用旧出口节点历史 IP；
  - 落地 IP 详情弹窗调整为长按触发，满足精细查看需求。
* **IPv6 路由规则配置与防泄露机制加固**：
  - VPN 虚拟网卡声明补齐 IPv6 虚拟 DNS 服务器地址，杜绝 Android netd 将 AAAA 解析旁路至物理网络造成泄漏；
  - 禁用 IPv6 模式下在内核路由顶层强制域名解析策略锁定为 `ipv4_only` 并拒绝 IPv6 流量及 AAAA 查询，杜绝远端节点建立双栈出站连接；
  - 仅 IPv6 模式下强制单栈 `ipv6_only` 解析并阻断 IPv4 流量。
* **单节点删除“双重确认”弹窗 Bug 消除**：
  - 移除操作菜单与删除方法之间的重复确认对话框，收口为单次确认；
  - 保留对正在运行中节点的删除拦截与友好提示。
* **“启用 selector（免重载切换节点）”开关与配置联动修复**：
  - 统一 selector、urltest 与 loadbalance 出站标签为 `TAG_PROXY` ("proxy")，与 Go 核心层严格对齐；
  - Go 内核层增加 selector 实例自动遍历查找容错；
  - 分组配置保存后若服务处于运行中，自动联动触发内核平滑重载。
* **观测间隔 (Interval) 数值单位与 UI 提示规范**：
  - 全面规范所有多语言 strings 与设置界面提示为“观测间隔 (秒)”；
  - 校准默认值为 300 秒，下发内核配置时加入 10 秒下限安全保护，防止高频测速打满连接。
* **测速链路与实际网络连通性对齐**：
  - 测速模式下保留 `dns-remote` 经节点自身出站远端解析，消除本地 ISP DNS 污染造成的测速假死或本地解析成功的假通；
  - Go 内核层严格校验 HTTP 响应状态码，状态码 `>= 400` 时明确抛出错误，防止报错页面被误判为极低延迟。

# Own for Android v2.6.0 正式版

* 增加应用名称正式更名为 Own
* 增加设置页面分类折叠与手风琴展开交互
* 增加应用图标与通知栏快捷开关自适应系统深浅色主题
* 增加多规格纯净液态玻璃桌面快捷开关小组件
* 增加主页置顶机场订阅资产信息卡片
* 增加全自由自定义主题色与纯白模式
* 优化 URL Test 测速与网络握手延迟准确度，消除虚高
* 优化控制中心快捷开关磁贴图标尺寸适配
* 优化主页底部落地 IP 与实时速率排版
* 优化节点卡片操作菜单与重复节点清理逻辑
* 修复 Hysteria 2、TUIC、WireGuard 协议连通性与测速异常
* 修复批量更新订阅时的并发冲突与闪退崩溃
* 修复剪贴板导入节点时的强制去重缺陷
* 修复禁用 IPv6 时的流量与 DNS 泄露问题
* 隐藏设置中冗余的“忽略电池优化”选项

## 核心修复与体验重构

* **系统控制中心（Tile）快捷开关图标放大适配**：
  - 针对原生四柱主体在通知栏磁贴圆圈中偏小的问题，进行精细边界提取与居中放大重绘，将四柱主体高度占比提升至 ~76%（居中约 390px，留白缩减至合理的 ~60px）；
  - 适配输出高对比、抗锯齿纯白单色透明矢量风格 `ic_throne_tile.png` 及 `ic_launcher_monochrome.png`，与系统手电筒、蓝牙、飞行模式等原生开关视觉尺寸完美平衡。
* **设置界面折叠式 UI 全面重构（参考 V2RayNG）**：
  - 将设置页面由扁平冗长的平铺列表重构为 8 个独立的折叠分类组：用户界面设置、VPN 设置、模式与入站设置、核心设置、DNS 设置、分片 (Fragment) 设置、连接观测设置 (Observatory)、进阶设置；
  - 引入原生支持展开/收起的分类组件 `ExpandablePreferenceCategory`，默认全部处于收起状态（一屏尽览，清爽规范，严格对齐 V2RayNG 交互标准）；
  - 支持手风琴/自由折叠交互与动态主题色箭头（`˅` / `˄`）指示；所有内部 Key、DataStore 持久化及子项动态联动规则完整保留无损。
* **“忽略电池优化”设置项的静默隐藏**：
  - 从设置页面的可视列表中彻底移除“忽略电池优化”选项，消除界面冗余；
  - 保持后台常驻与服务运行时的静默电池优化兼容检测机制，不在前台设置 UI 打扰用户。
* **全局软件 App 名称更改**：
  - 面向用户的显示名称全面由 `OwnBox` 统一更新为 `Own`（如桌面图标名称、应用长名称 `Own for Android`、小组件选择器标签 `Own 1×1 / 2×2 / 4×1 / 4×2`、崩溃日志标题等）；
  - 严格保持内部代码包名（`moe.nb4a` / `com.ownbox.app`）及代码仓库工程结构不变。

# Own for Android v2.5.8 预览版 (v2.5.8-preview)

## 核心修复与体验优化

* **全新应用默认图标与系统深浅色主题无缝自适应**：
  - 应用默认图标全面升级为全新四柱竖条设计（浅色模式采用纯白背景配海蓝/青蓝竖条，深色模式采用深空黑夜背景配亮蓝渐变竖条，出厂默认为浅色背景）。
  - 下拉控制中心（Quick Settings Tile）磁贴图标全品牌规范统一：升级为纯白单色剪影矢量透明风格（`ic_throne_tile`），与新版默认图标样式严格对齐。
  - 系统主题自适应无缝切换：当处于“动态”（默认）模式时，图标随系统深浅色主题无缝自动切换；通过 `DONT_KILL_APP` 标志位与异常防御性保护，切换过程丝滑流畅，绝不闪退崩溃，绝不打断或杀死后台正在运行的 VPN 服务。
* **清理“修改应用图标”设置中的冗余选项**：
  - 彻底清理移除万圣节、赛博朋克、粉红、德鲁伊、红色、俄罗斯方块、胡桃、芙宁娜、雷电将军、甘雨共 10 个冗余动漫/杂色图标选项及对应资源与清单声明；
  - 保持精简纯粹的 9 款核心质感图标：动态（自适应系统主题）、浅色模式、深色模式、旧版 NekoBox+、NekoBox、午夜、天空、黑白、文本。
* **全局 URL Test 节点测速延迟算法对齐与虚高消除**：
  - 重构内核测速探针，全面对齐 Throne 业界主流的两阶段长连接保活（Keep-Alive Warm Probe）算法；
  - 阶段一完成目标端建链与 TLS 握手并保留至连接池，阶段二通过复用长连接发起极轻量 HTTP HEAD 探测，测得纯 1-RTT 真实物理往返时延（~100-180ms），彻底消除冷启动建链开销造成的 350-500ms+ 虚高读数。
* **主页底部落地 IP 卡片 HTTP 握手延迟校准**：
  - 彻底解耦落地 IP 查询（GeoIP API 下载解析整体耗时）与网络握手延迟，防止 ~372ms 的 API 接口解析耗时污染握手延迟指标；
  - 统一由 `urlTest()` 内核探针精确测量真实的 HTTP/HTTPS 握手往返延迟，使主页底部卡片延迟回归真实的 150-170ms 区间（严格对齐 Throne 的 163ms 表现）。


## 核心修复与体验优化

* **HY2 节点 TCP Ping 与 URL Test 连通性彻底修复**：
  - 剔除 `HysteriaFmt` 中硬编码的错误 `alpn = ["h3"]` 配置，解除与 Hysteria 2 协议标准 ALPN 冲突导致的 `EOF` 握手阻断；为 Hysteria 1/2 与 TUIC 全面引入 `serverAddress` 智能 SNI 兜底策略。
  - 废弃已失效且会产生 3 秒超时挂起的伪造 QUIC 协商包探测，对纯 UDP 协议节点（HY2、TUIC、WireGuard）直接透传由 URL Test 高效测试，彻底消除 UDP 协议探测黑洞。
* **URL Test / TCP Ping 延迟严重虚高彻底消除与算法对齐**：
  - 移除 URL Test 启动前针对测试目标域名在未代理环境下的主线程阻塞式 DNS 预解析（`InetAddress.getAllByName`），消除测速启动排队与首节点卡死问题。
  - 内核层（libcore）测速机制无缝升级为官方 `sing-box` 原生 `urltest.URLTest` 链路标准（结合 `NeedHandshakeForWrite` 与高效 HTTP HEAD 探针），仅测量真实的代理网络往返首包延迟（TTFB），彻底消除 2000ms+ 虚高读数，测速数值精准对齐主流客户端（~150-300ms）。
  - TCP Ping 计时严格隔离：将 `InetSocketAddress` 域名解析前置于计时器外，仅精确测量纯 TCP 三次握手 RTT 时间；并为测速并发协程增加超时安全保护。
* **落地 IP 详情弹窗底部按钮颜色冲突与对比度优化**：
  - 彻底重构“复制信息”、“重新测速”及“完成”底部按钮的主题色与文字对比度适配；
  - 描边线框按钮采用清晰的主题色高对比文字、图标与边框，主要操作按钮采用主题色背景搭配白色文字，全面消除浅色、纯白、深色及 AMOLED 黑色主题下的按钮隐形与不可见问题。

# OwnBox for Android v2.5.6 预览版 (v2.5.6-preview)

## 核心修复与体验升级

* **主页底部落地 IP 卡片加高与两行排版**：底部状态栏高度参照 Throne 适度加高，右侧延迟全新重构为清晰的上下两行排版（上行严格独立展示“HTTP/HTTPS 握手延迟”，下行加粗展示具体延迟数值），彻底解决系统大字号下的文本截断与字串拼接错位。
* **路由与规则资源更新 403 频控绕过与提示**：直连 GitHub Releases 官方下载地址，彻底绕过 GitHub REST API 60次/小时的未认证 403 速率限制，并补充完善检查更新、更新成功与失败的友好 Toast 状态通知。
* **节点去重逻辑重构（保留首个/选中项）**：彻底重构右上角“删除重复的服务器”功能，保留各重复组的首个（或当前选中的）节点，仅安全剔除多余冗余节点，并平滑迁移当前选中状态与同步刷新列表。
* **URL Test 测速延迟虚高与算法对齐**：全面对齐官方 sing-box 及 Throne 测速标准，改用显式出站拨号建立隧道后单次 HTTP GET 精确测量 TTFB 延迟（~150-350ms），彻底消除包含冷启动建链的 1000~2000ms+ 虚高读数。
* **Hysteria2 与 TUIC 测速 EOF 及死锁修复**：消除人工设置的 `udpSemaphore(2)` 并发瓶颈，加入超时熔断保护，防止异常节点阻塞整队；彻底杜绝在已关闭 QUIC 流上重用连接导致的 EOF 伪失败。
* **Trojan 节点 URL Test 可用性修复**：修复构建 Trojan 出站时缺失 TLS enabled 与 server_name/SNI 导致的握手协商失败。
* **IPv6 禁用开关泄露与黑洞拦截**：解决 Android VPN 服务未捕获 IPv6 导致的物理网卡直连泄露，在 VPN 顶层完整接管 IPv6 路由与地址，并在 sing-box 内核层严格阻断并丢弃所有 IPv6 流量及 AAAA 查询。
* **极端白底与纯黑/AMOLED 主题对比度校准**：全面优化落地 IP 详情弹窗等界面在纯白和暗黑/AMOLED 模式下的背景、边框与文字对比度，文字清晰锐利易读。

# OwnBox for Android v2.5.5 预览版 (v2.5.5-preview)

## 核心修复与生命周期稳定性

* **订阅批量更新闪退修复 (Fragment Lifecycle)**：彻底修复在订阅分组列表点击“一键更新所有订阅”时，因用户离开页面或页面切换导致 Fragment 解除绑定（Detached）而抛出 `java.lang.IllegalStateException: Fragment not attached to an activity` 的致命闪退问题。
* **全局异步通知与生命周期防御性加固**：全面重构 Fragment 提示组件，新增 `safeSnackbar` 安全派发机制与全局活动回退兜底，针对所有后台并发任务、导入导出操作实施全方位生命周期安全保护，杜绝任何页面脱离导致的空指针与状态异常崩溃。

# OwnBox for Android v2.5.4 预览版 (v2.5.4-preview)

## 核心修复与体验优化

* **剪贴板导入去重开关修复**：严格遵循设置中“去重（更新时移除重复配置）”开关状态，关闭时从剪贴板导入节点不再强制去重，彻底保留完整节点。
* **一键更新并发与异常隔离**：重构订阅列表“一键更新所有订阅”机制，改为多线程高并发同时更新，并引入单个订阅异常独立隔离保护，避免单链网络超时导致批处理崩溃中断。
* **URL Test 测速超时全面修复**：优化节点测速内核调用与预热逻辑，采用完整单轮端到端测量并在预热超时异常时自动无缝兜底，彻底解决测速大面积报 context deadline exceeded 缺陷。
* **IPv6 禁用开关彻底生效**：修复在设置中禁用 IPv6 路由解析失效的问题，全面强制 DNS 采用 ipv4_only 策略并拦截 AAAA 查询与 IPv6 路由规则。
* **节点扫描完整度校准**：移除与官方 NekoBox 冲突的节点名称过滤规则，杜绝有效订阅节点被错误误杀或吞掉，确保扫描入库数量完全一致。
* **Cloudflare Shadowsocks 连通性支持**：完善 SS v2ray-plugin 协议参数构造逻辑，规范注入 `;mux=0` 关键参数，彻底恢复 Cloudflare SS 节点的可用性。
* **主页置顶资产卡片数据严格同步**：统一主页置顶信息卡片与侧边栏分组列表的流量计算公式与 Room 实体数据源，彻底解决流量数据不同步与脱节问题。
* **主页底部状态栏排版防溢出**：加宽落地 IP 卡片可用宽度并微调上下行速率弹性权重与字号自适应区间，彻底杜绝高速下载时下行速率文字超出面板右侧边界。
* **节点操作交互升级**：移除易造成误触的长按手势操作，在每个节点卡片右侧增设明确的“三个点（...）”操作按钮，点击即呼出节点管理操作弹窗。
* **清理单分组冗余设置项**：彻底移除单个订阅分组设置中无实际作用的“自动优选 / URLTest”与“负载均衡 / Load Balance”冗余选项及相关配置绑定。

# OwnBox for Android v2.5.3 预览版 (v2.5.3-preview)

## 核心修复与稳定性

* **修复批量更新订阅异常中断**：修复订阅列表点击“一键更新所有订阅”因单个订阅连接异常导致整体批处理中断报错的问题，实施循环内异常隔离机制并支持代理拉取失败自动无缝回退本地直连。
* **修复主页底部高速下行排版溢出**：重构主页底部落地 IP 状态栏为对称三段式弹性排版，增设中央悬浮按钮物理避让间距与动态字号缩放，彻底杜绝高速下载时下行速率文本超出面板右侧边界的排版 Bug。
* **校准主页置顶卡片流量资产数据**：重构流量解析优先级，优先读取订阅节点下发的真实用户流量信息（`Subscription-Userinfo`），彻底解决主页置顶卡片已用流量显示为 0 Bytes 的数据脱节问题，与侧边栏分组列表数据严格对齐。

# OwnBox for Android v2.5.2 预览版 (v2.5.2-preview)

## 核心修复与体验优化

* **修复 HTTPS 测速 UNAVAILABLE 缺陷**：修复测速设置使用 HTTPS 链接（带 S）时右上角 URL 测试报 UNAVAILABLE 的问题，底层对齐 NekoBox 官方实现并支持双重回退兜底机制。
* **重构 URL Test 测速算法**：全面对齐 NekoBox 官方首字节计时 (TTFB)，在保证全协议高精度的同时还原真实低延迟。
* **重构主页底部落地 IP 卡片排版**：全面优化状态栏为两行式优雅紧凑布局，左侧并列网速、右侧延迟握手、下方独占全宽落地 IP，彻底解决文字截断与省略号挤压问题。
* **侧边栏设置全局默认 UA 出厂预设**：出厂预设设为高兼容稳定版（`NekoBox/Android/1.4.2 (Prefer ClashMeta Format)`），保留自由修改与重置能力，并加入订阅更新节点防误删安全保护机制。
* **卡片精简与资产数据统一**：彻底移除主页置顶卡片中的冗余进度条，统一流量数据展示单位与侧边栏分组列表一致，回滚侧边栏分组列表至初始稳定代码。
* **修复剪贴板导入去重逻辑缺陷**：修复从剪贴板导入订阅/节点时，即便关闭“去重”开关仍被强行去重的缺陷，严格遵循用户去重开关状态。
* **重构“去除重复的服务器”功能**：彻底解决右上角菜单“去除重复的服务器”点击无响应问题，实现基于物理连接特征的精准去重与数据库安全联动。

# OwnBox for Android v2.5.1 预览版 (v2.5.1-preview)

## 核心修复与稳定性

* **分组标签滑动防回弹**：彻底修复在 VPN 运行中或启用负载均衡时，主页顶部左右滑动切换订阅分组被后台轮询与状态变更强制回弹原分组的问题，保障标签滑动顺畅自如。
* **状态卡片三行紧凑排版**：全面重构主页底部 Landing IP 状态栏为紧凑三行式布局，消除小屏或长地名下 HTTPS 握手延迟被文字截断或省略号遮挡的缺陷。
* **Hysteria2 与 TUIC 测速修复**：修复 Hysteria2 与 TUIC 协议节点在 HTTPS 测速下因 ALPN/H2 升级异常而错误显示 Unavailable 的缺陷，支持 UDP Fragment 与平滑回退。
* **UDP 协议 TCP Ping 探测支持**：针对 Hysteria2、TUIC 等纯 UDP 协议，新增 QUIC Version Negotiation 握手探测与物理 RTT 测算，并在超时后平滑兜底，彻底解决 TCP Ping 3000ms 超时报错。
* **去重功能深度修复**：彻底修复主页右上角菜单“去除重复的服务器”点击无响应的 Bug，剥离节点名称差异比较协议核心连接指纹，精准定位当前浏览分组并提供友好操作反馈。
* **订阅资产卡片与 UA 同步**：重构订阅资产卡片为三行对称排版，分离并完整展示“可用流量”与“已用流量”；打通节点回退提取与 Room 数据库双向持久化，消除主页卡片与抽屉分组数据脱节；内置预设新增推荐 UA（`NekoBox/Android/1.4.2 (Prefer ClashMeta Format)`）。
* **URL Test 测速算法官方对齐**：对标 NekoBox 官方测速核心，实施 TTFB（首包 Header 到达）即刻截断并切断 Body 读取，消除原本五六百至上千毫秒的虚高延迟，还原节点最真实网络质量。
* **机场名称智能识别**：完善订阅导入与更新时的机场名称提取逻辑，优先解析 RFC 5987 文件名与 User-Agent/URL 标识，彻底杜绝残留的“MY GROUP”默认分组名。

# OwnBox for Android v2.5.0

## 全新功能

* 纯净液态玻璃多规格桌面小组件
* 流媒体、AI 与网络服务解锁检测全链路重构
* 网络安全与链路审计探测（Google 送中与 GFW 阻断检测）
* 订阅分组顶部资产信息卡片
* 全自由 HEX/RGB 自定义主题色与极简纯白模式

## 交互与体验

* 顶栏长按直达分组设置
* 节点列表卡片精简与操作弹窗重构
* 订阅分组独立 User-Agent 设置与防覆盖锁定
* 失效节点自动软屏蔽
* 设置页偏好项视觉规范化（重构“修改应用图标”展示，移除突兀大色块）

## 核心修复与稳定性

* VPN 核心长效后台保活
* 负载均衡流量统计校准
* 订阅资产卡片解析校准与回退探测
* 订阅导入防重复与自动改名
* 内核协议解析与连通性测试校正
* Room 数据库平滑迁移

